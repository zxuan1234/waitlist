# Project URL from the green Connect button (https://xxxx.supabase.co).
# Browser / Netlify: publishable or Legacy anon JWT only.
SUPABASE_URL=
SUPABASE_ANON_KEY=
# n8n only — never Netlify, never the page. Legacy service_role JWT or sb_secret_.
SUPABASE_SECRET_KEY=
